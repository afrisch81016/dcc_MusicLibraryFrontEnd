import "./NavBar.css"

const NavBar = (props) => {

    return (
        <div className="navbar">
            <span className='musicspan'>Music</span><span className='planetspan'>Planet</span>
        </div>

    );
}

export default NavBar